 Get_shader_format ← {
    os ← ('Mac' ⋄ 'Linux' ⋄ 'Unsupported')
    formats ← (
        SDL_GPU_SHADERFORMAT_MSL
        SDL_GPU_SHADERFORMAT_SPIRV
        SDL_GPU_SHADERFORMAT_INVALID
    )
    formats⌷⍨os⍳⊂'-64'~⍨⊃#⎕WG'APLVersion'
 }

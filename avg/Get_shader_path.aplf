 Get_shader_path←{
     ⍝ ⍵ ←→ File name
     ⍝ e.g., 'basic_vert' → 'shaders/msl/basic_vert.msl' on macOS
     os←('Mac' ⋄ 'Linux' ⋄ 'Windows')
     paths←(
         'shaders/msl/'
         'shaders/spv/'
         'shaders/dxil/'
     )
     ext←(
         '.msl'
         '.spv'
         '.dxil'
     )
     idx←os⍳⊂'-64'~⍨⊃# ⎕WG'APLVersion'
     ∊paths[idx],⍵,ext[idx]
 }

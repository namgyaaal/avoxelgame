 Create_depth_texture←{
    ⍝ ⍺ ←→ (Width, Height)
    ⍝ ⍵ ←→ GPU Device
     (w h)←⍺
     device←⍵
     format←Get_depth_format device
     params←⊂0 format 4 w h 1 1 0 0
     SDL_CreateGPUTexture device params
 }

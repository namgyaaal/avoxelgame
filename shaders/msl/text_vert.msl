#include <metal_stdlib>
#include <simd/simd.h>

using namespace metal;

struct MVPBlock
{
    float4x4 mvp;
};

struct main0_out
{
    float4 o_color [[user(locn0)]];
    float2 o_uv [[user(locn1)]];
    float4 gl_Position [[position]];
};

struct main0_in
{
    float3 position [[attribute(0)]];
    float4 color [[attribute(1)]];
    float2 uv [[attribute(2)]];
};

vertex main0_out main0(main0_in in [[stage_in]], constant MVPBlock& _30 [[buffer(0)]])
{
    main0_out out = {};
    out.o_color = in.color;
    out.o_uv = in.uv;
    out.gl_Position = _30.mvp * float4(in.position, 1.0);
    return out;
}

